<%#= form_with scope: :follow, url: follows_path, local: true do |form| %>
  <%#= form.label :followed_user_id %>
  <%#= form.text_field :followed_user_id %>
  <%#= form.submit "Follow" %>
<%# end %>


<%= link_to user_follows_path(@user), data: { turbo_method: :post } do %>
  <div class="btn btn-success"></div>
<% end %>

<%= link_to user_follow_path(@follow), data: { turbo_method: :delete } do %>
  <div class="btn btn-danger"></div>
<% end %>
